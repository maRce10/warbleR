NEWS
Version 1.0.0 2015-03-06

Changes:
  1. Created R package "warbleR". Organized the files.

  2. Put the required packages out of the function, R will automatically warn user to install the package.

  3. manualoc():
    i. Locator process: Duplecate comparison.

  4. Added progress bar in querxc, mp32war, lspec using "pbapply" package.

  5. Use message() to show some message instead of warning().