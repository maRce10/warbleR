# extract method for class `extended_selection_table`

extract method for class `extended_selection_table`

## Usage

``` r
# S3 method for class 'extended_selection_table'
X[i = NULL, j = NULL, drop = FALSE]
```

## Arguments

- X:

  Object of class `extended_selection_table`, generated by
  [`selection_table`](https://marce10.github.io/warbleR/reference/selection_table.md).

- i, :

  j, indices specifying elements to extract or replace. Indices are
  numeric or character vectors or empty (missing) or NULL.
